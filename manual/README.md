Group shapes are created manually in diagrams.net
from architecture groups icons and exported.

A single shape is a rectangle and an icon, grouped.
The icon has disabled properties: `moveable`, `resizable`.
